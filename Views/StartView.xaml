<UserControl x:Class="Hangman_Game.Views.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- L-Side: User List -->
            <Grid Grid.Column="0" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="260"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Users" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Grid.Row="1" Text="No users found. Please create one." Foreground="Gray" 
                           Visibility="{Binding HasUsers, Converter={StaticResource InverseBoolToVis}}" Margin="0,0,0,5"/>
                <ListBox Grid.Row="2" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" Margin="0,0,0,0" VerticalAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="2">
                                <Image Source="{Binding ImagePath, Converter={StaticResource ImagePathConverter}}"
                                       Width="32" Height="32" Margin="0,0,10,0" Stretch="UniformToFill"/>
                                <TextBlock Text="{Binding Username}" VerticalAlignment="Center" FontSize="16"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <!-- R-Side: Avatar Frameworks -->
            <Grid Grid.Column="1" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="260"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Select Avatar:" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

                <Grid Grid.Row="1" Margin="0,5,0,0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Command="{Binding PreviousAvatarCommand}" Content="&lt;&lt;" Width="50" Margin="0,0,5,0" ToolTip="Previous Avatar" FontSize="20" HorizontalAlignment="Left" VerticalAlignment="Stretch"/>
                    
                    <Border Grid.Column="1" BorderBrush="LightGray" BorderThickness="1" Margin="0,0,5,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <Image Stretch="Uniform" ToolTip="Preview Avatar" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Image.Source>
                                <Binding Path="CurrentAvatarPath" Converter="{StaticResource ImagePathConverter}" TargetNullValue="pack://application:,,,/Assets/default.jpg" FallbackValue="pack://application:,,,/Assets/default.jpg"/>
                            </Image.Source>
                        </Image>
                    </Border>
                    
                    <Button Grid.Column="2" Command="{Binding NextAvatarCommand}" Content="&gt;&gt;" Width="50" Margin="0,0,5,0" ToolTip="Next Avatar" FontSize="20" HorizontalAlignment="Left" VerticalAlignment="Stretch"/>
                    
                    <Button Grid.Column="3" Command="{Binding SelectAvatarCommand}" Content="Select" Width="100" Margin="0,0,0,0" ToolTip="Use this avatar" FontSize="20" HorizontalAlignment="Left" VerticalAlignment="Stretch"/>
                </Grid>
            </Grid>
        </Grid>

        <!-- Bottom Section: Creation and Actions -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="0,1,0,0" Margin="10,10,10,0" Padding="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center" Margin="0,0,15,0">
                    <TextBlock Text="Create New User" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Username:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}" Width="150" VerticalAlignment="Center" Margin="0,0,10,0">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding CreateUserCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                        <Button Command="{Binding CreateUserCommand}" Content="Create User" Width="100" Margin="0,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" FontSize="12" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Command="{Binding DeleteUserCommand}" Content="Delete User" Width="100" Margin="10,0"/>
                    <Button Command="{Binding PlayCommand}" Content="Play" Width="100" Margin="10,0"/>
                    <Button Command="{Binding CancelCommand}" Content="Cancel" Width="100" Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
