<UserControl x:Class="Hangman_Game.Views.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- L-Side: User List -->
            <StackPanel Grid.Column="0" Margin="10">
                <TextBlock Text="Users" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="No users found. Please create one." Foreground="Gray" 
                           Visibility="{Binding HasUsers, Converter={StaticResource InverseBoolToVis}}" Margin="0,0,0,5"/>
                <ListBox ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" DisplayMemberPath="Username" Margin="0,0,0,10" Height="200" VerticalAlignment="Top"/>
            </StackPanel>

            <!-- R-Side: Avatar Frameworks -->
            <StackPanel Grid.Column="1" Margin="10">
                <TextBlock Text="Select Avatar:" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,0" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <Button Command="{Binding PreviousAvatarCommand}" Content="&lt;&lt;" Width="50" Height="200" Margin="0,0,20,0" ToolTip="Previous Avatar" FontSize="20"/>
                    <Border BorderBrush="LightGray" BorderThickness="1" Width="200" Height="200">
                        <Image Stretch="Uniform" ToolTip="Preview Avatar">
                            <Image.Source>
                                <Binding Path="CurrentAvatarPath" Converter="{StaticResource ImagePathConverter}" TargetNullValue="pack://application:,,,/Assets/default.png" FallbackValue="pack://application:,,,/Assets/default.png"/>
                            </Image.Source>
                        </Image>
                    </Border>
                    <Button Command="{Binding NextAvatarCommand}" Content="&gt;&gt;" Width="50" Height="200" Margin="20,0,20,0" ToolTip="Next Avatar" FontSize="20"/>
                    <Button Command="{Binding SelectAvatarCommand}" Content="Select" Width="100" Height="200" ToolTip="Use this avatar" FontSize="20"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Bottom Section: Creation and Actions -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="0,1,0,0" Margin="10,10,10,0" Padding="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Create New User" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBlock Text="Username:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}" Width="150" VerticalAlignment="Center" Margin="0,0,10,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding CreateUserCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Command="{Binding DeleteUserCommand}" Content="Delete User" Width="100" Margin="10,0"/>
                    <Button Command="{Binding PlayCommand}" Content="Play" Width="100" Margin="10,0"/>
                    <Button Command="{Binding CancelCommand}" Content="Cancel" Width="100" Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
